version: 2

sources:
  - name: hr
    database: dbt
    schema: hr
    description: "hr-related raw data"

    tables:
      - name: employees
        columns:
          - name: employee_id
            tests:
              - not_null
      - name: departments